name: my_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 👇 FIXED INDENTATION: fl_chart is now at the top level 👇
  fl_chart: ^0.69.0

  # Firebase
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.4
  cloud_firestore: ^6.6.0
  google_sign_in: ^6.1.5

  # Utilities
  csv: ^6.0.0
  file_picker: ^8.3.7
  universal_html: ^2.2.4

  # Google Sheets
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0
  http: ^1.6.0

  cupertino_icons: ^1.0.8
  spreadsheet_decoder: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true